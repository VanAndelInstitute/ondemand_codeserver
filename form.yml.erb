---
cluster: "hpc"
form:
  - bc_num_hours
  - working_dir
  - sh_partition

attributes:

  working_dir:
    label: "Working Directory"
    data-filepicker: true
    data-target-file-type: dirs  # Valid values are: files, dirs, or both
    readonly: false
    help: "Optional: Select a project directory to open; defaults to your /home/user.name if not specified"

  sh_partition:
    label: "Partition"
    value: "short"
    required: true
    help: "For example: quick short bigmem long, or your private nodes."

  bc_num_hours:
    label: "Runtime (in hours)"
